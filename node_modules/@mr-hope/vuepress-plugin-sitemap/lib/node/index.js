"use strict";
const chalk_1 = require("chalk");
const sitemap_1 = require("./sitemap");
const sitemapPlugin = (options, context) => {
    if (!options.hostname) {
        console.log((0, chalk_1.blue)("Sitemap"), chalk_1.black.bgRed("Error"), 'Not generating sitemap because required "hostname" option doesn’t exist');
        return { name: "sitemap" };
    }
    return {
        name: "sitemap",
        async generated() {
            await (0, sitemap_1.genSiteMap)(options, context);
        },
        plugins: [["@mr-hope/git", true]],
    };
};
module.exports = sitemapPlugin;
//# sourceMappingURL=index.js.map